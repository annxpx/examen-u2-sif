export interface Attempt_JSON {
    attempts: number;
    word: string;
    message: string;
  }
  
  export type Attempt ={
    attempts: number;
    word: string;
    message: string;
  }
  export interface s_word_JSON {
    word: string;
  }
  
  export type s_word ={
    word: string;
  }

  export interface letter_JSON {
    letter: string;
  }
  
  export type letter ={
    letter: string;
  }