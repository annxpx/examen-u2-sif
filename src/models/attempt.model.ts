export interface Attempt_JSON {
    attempts: number;
    word: string;
    message: string;
    letter: string;
  }
  
  export type Attempt ={
    attempts: number;
    word: string;
    message: string;
    letter: string;
  }
  export interface s_word_JSON {
    word: string;
  }
  
  export type s_word ={
    word: string;
  }